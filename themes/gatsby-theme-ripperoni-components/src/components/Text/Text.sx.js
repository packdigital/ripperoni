/* eslint-disable max-lines */
/* eslint-disable import/no-default-export */

import { numberOrStringOrArray } from '../../props/types';


const sx = [];

const alias = {
  size: 'fontSize',
};

const computed = {};

const propTypes = {
  size: numberOrStringOrArray,
};

export default {
  sx,
  computed,
  alias,
  propTypes,
};
