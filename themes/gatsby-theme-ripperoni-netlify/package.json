{
  "name": "@packdigital/gatsby-theme-ripperoni-netlify",
  "version": "0.0.13",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "clean": "gatsby clean",
    "reset": "git clean -dfx && git reset --hard && yarn",
    "version": "standard-version",
    "prepare-release": "run-s reset version",
    "publish-release": "yarn prepare-release && git push --follow-tags origin master && yarn publish --non-interactive"
  },
  "author": "@packdigital",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/packdigital/gatsby-theme-ripperoni-netlify.git"
  },
  "prettier": "@packdigital/prettier-config-ripperoni",
  "dependencies": {
    "gatsby-plugin-netlify": "^2.3.10",
    "http-proxy-middleware": "^1.0.4"
  },
  "peerDependencies": {
    "@packdigital/ripperoni-utilities": "^2.0.21",
    "gatsby": "^2.19.5",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "netlify-plugin-gatsby-cache": "^0.3.2"
  }
}
